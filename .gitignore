# 本地研究/测试产物（不部署）
_research/
node_modules/

# WorkBuddy 项目元数据
.workbuddy/

# 系统/编辑器
.DS_Store
Thumbs.db
*.log

# 部署打包产物（本地生成，不入库）
our-planet.zip
.deploy/
.wrangler/

# 含密钥的 Worker 配置（明文 SYNC_KEY，禁止入库）
worker/wrangler.toml
# 站点同域同步后端（functions/ + 根 wrangler.toml）仅用于本地 wrangler pages deploy 直接上传，
# 不入库：Pages git 构建不支持在仓库内这样配置 KV 绑定（会导致构建失败）
functions/
wrangler.toml
# wrangler 缓存（含账户信息，不入库）
worker/.wrangler/
