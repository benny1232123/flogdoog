name = "flogdoog-sync"
main = "sync.js"
compatibility_date = "2024-09-23"

# KV 命名空间绑定：把下面 id 换成你在 Cloudflare 控制台创建的命名空间 ID
[[kv_namespaces]]
binding = "LP_KV"
id = "REPLACE_WITH_YOUR_KV_NAMESPACE_ID"

# 同步密钥：建议用机密方式设置（见 README），此处占位仅为本地可跑
[vars]
SYNC_KEY = "REPLACE_WITH_A_STRONG_SECRET"
