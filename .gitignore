# 本地研究/测试产物（不部署）
_research/
node_modules/

# WorkBuddy 项目元数据
.workbuddy/

# 系统/编辑器
.DS_Store
Thumbs.db
*.log

# 部署打包产物（本地生成，不入库）
our-planet.zip
.deploy/
.wrangler/

# 含密钥的 Worker 配置（明文 SYNC_KEY，禁止入库）
worker/wrangler.toml
# 注：根目录 wrangler.toml 含 Pages Functions 的 KV 绑定，且 SYNC_KEY 已随前端公开，故允许入库以让 git 构建带上同步后端
# wrangler 缓存（含账户信息，不入库）
worker/.wrangler/
